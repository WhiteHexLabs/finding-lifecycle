# finding-lifecycle：智能合约漏洞发现后处理工作流设计

## 1. 总体方案与设计调整

采用 **通用 Skill + Markdown Ledger + YAML 计划配置 + Python gate CLI**。保留七阶段，不绑定 security-auditor、solidity-auditor 或 0xsimao 的输出格式；入口只要求能提取漏洞主张、目标部署、来源及已有证据。

本版以 EVM 已部署合约为实证对象，默认使用 Foundry 主网 fork。框架无关指审计入口无关；暂不实现其他链的执行适配器。

相对原方案，作以下调整：

| 原设计 | 调整 |
|---|---|
| 第 3 阶段才检查资格 | 登记后先做资格预筛，第 3 阶段结合实际损害正式定级，提交前复查规则 |
| 一个状态字段承载全部情况 | 分离处理阶段、结案结果和阻塞原因，避免把 RPC 故障当作误报 |
| 每次迁移同步宿主 memory | 更新仓库内派生索引，ledger 是唯一事实源，索引可重建 |
| `REVIEWED_SAFE` | 改为 `REFUTED`，限定为该 finding 在指定部署、区块和前提下被推翻 |
| secret gist 交付 | 禁止使用；仅允许私有仓和平台私密附件等受控渠道 |
| `ESCALATED` 与接受、拒绝并列 | 申诉作为提交后的独立跟踪状态，不覆盖平台原始决定 |
| 测试全绿即可过门 | 测试结果与有证据引用的实质复核必须同时满足 |

坚持两条红线：**不修目标协议代码；不公开披露。** 修复建议仅进入报告附件；PoC 使用独立攻击合约和测试，不修改目标实现来制造结果。

## 2. 持久化结构与状态契约

### 2.1 文件组织

Skill 仓库与实际漏洞数据分离，安装 Skill 不携带项目漏洞资料。

```text
finding-lifecycle/
├── SKILL.md
├── references/
│   ├── workflow.md
│   └── contracts.md
├── templates/
│   ├── program.yaml
│   ├── finding.md
│   ├── assessment.md
│   ├── report.en.md
│   ├── self-review.md
│   ├── appeal.en.md
│   └── poc/
│       └── setup_and_run.sh
├── scripts/
│   └── lifecycle.py
└── tests/
    └── test_lifecycle.py
```

每个目标计划使用一个独立工作目录：

```text
<case-root>/
├── program.yaml
├── findings/
│   └── F-<uuid>.md
├── evidence/
│   └── F-<uuid>/
├── packages/
│   └── F-<uuid>/
└── index.md
```

`evidence/` 保存源码核验、链上取证、规则快照、运行日志及回执。`packages/` 保存冻结后的报告与 PoC 包。凭据、RPC 密钥和 KYC 身份材料不进入这些目录。

### 2.2 Ledger

一条 finding 一个 Markdown 文件。frontmatter 保存可检查字段，正文保存主张、分析与证据解释。

核心字段：

| 字段 | 含义 |
|---|---|
| `id`、`title`、`program_id` | 稳定身份；ID 不包含 severity，降级不改 ID |
| `sources` | 原审计器、原 finding ID、审计轮次、源文件及内容哈希 |
| `stage` | 最近通过的处理阶段 |
| `disposition` | 当前结案结果，默认 `OPEN` |
| `revision` | CLI 每次写入递增，防止并发覆盖 |
| `targets` | chain ID、地址、代理及实现地址、区块和代码哈希 |
| `root_cause` | 根因主张及相关变体 |
| `duplicate_of` | 合并后的主 finding ID |
| `severity` | 候选等级、最终等级、矩阵条目和论证引用 |
| `program_snapshot` | 本次资格判断所依据的规则快照哈希 |
| `evidence` | 产物路径、SHA-256、用途和生成记录 |
| `gates` | gate 结果、输入摘要、复核者、时间及理由 |
| `blockers` | 未解决事项、缺失证据及下一步 |
| `submission` | 人工提交回执、平台编号、提交时间和包哈希 |
| `appeal` | 申诉状态、截止时间、材料及发送回执 |
| `history` | 迁移、重开、失效、结果变更的追加记录 |

正常处理阶段：

```text
DISCOVERED → CROSS_CHECKED → FORK_PROVEN → TRIAGED
→ PACKAGED → SELF_REVIEWED → SUBMITTED
```

结案结果独立记录：

```text
OPEN
MERGED
INELIGIBLE
REFUTED
ACCEPTED
REJECTED
WITHDRAWN
```

规则：

- `MERGED` 必须指向存在的主 finding，禁止自引用和合并环；保留原始来源。
- `INELIGIBLE` 表示不符合 bounty 资格，不表示漏洞不存在。
- `REFUTED` 必须有 fork 反证和明确的结论边界；静态怀疑被推翻但尚未 fork，只能保持待取证。
- RPC 不可用、规则不明确、依赖缺失均记录为 blocker，不结案。
- 平台接受或拒绝只反映平台结果，不自动改写技术判断。
- 重开必须保留原结案事件，记录新证据，指定重新进入的阶段，并使后续 gate 失效。

申诉状态为 `NONE / DRAFTED / SENT / RESOLVED`。已拒绝的报告可以处于申诉中；只有新的平台决定才能更新结案结果。

### 2.3 写入与恢复

所有正式迁移通过 CLI：

1. 获取工作目录写锁并检查预期 `revision`。
2. 重新验证前置 gate 和证据哈希。
3. 在同一个 ledger 文件内更新状态、gate 和 history。
4. 临时文件写入后原子替换 ledger。
5. 重建 `index.md`。

索引失败不回滚已经成功的 ledger 写入；返回明确的“ledger 已保存、索引待修复”。恢复时重建索引，不能重复迁移。

手工修改不直接赋予过门资格。恢复命令检查内容和历史一致性，发生冲突就阻塞。该机制防止操作失误，不宣称能防御有文件写权限者的蓄意篡改。

索引只包含 ID、标题、阶段、结果、阻塞原因和下一步，不复制技术证据，不写入宿主全局 memory。

## 3. program.yaml 与七阶段门槛

### 3.1 计划配置

`program.yaml` 包含：

- **计划身份与来源**：计划 ID、官方规则链接、抓取时间、有效时间、快照路径与哈希。
- **范围**：链、地址、代理关系、资产类型和允许的影响类别。
- **定级矩阵**：规则条目 ID、原文、等级及判断依据。
- **排除条款**：特权、MEV、timing、第三方依赖及其他计划条款。
- **查新来源**：历史审计、known issues、历史 finding、部署 era map；没有材料时显式记录缺失。
- **赏金规则**：计划公布的金额与币种，以及用户可选的最低提交收益要求；二者分开。
- **提交限制**：账号适用范围、计数对象、时间窗口、次数和时区。
- **交付要求**：语言、字段、附件限制、受控私密渠道。
- **KYC 与响应规则**：是否要求 KYC、官方 SLA、申诉方式和期限；未知值保留为空。

资格判断逐项记录 `PASS / FAIL / UNKNOWN / NOT_APPLICABLE`，每项附证据及解释。`UNKNOWN` 阻塞正式资格 gate；`NOT_APPLICABLE` 必须有理由。

脚本核验记录完整性、规则引用和明确的地址匹配，不自动解释自然语言排除条款。配置中不支持执行任意 Python 或 shell 表达式。

### 3.2 七阶段

| 阶段 | 动作与产物 | 出门条件 |
|---|---|---|
| **0 登记** | 分配 ID，保留原始 finding；对照历史 finding 和 era map 去重；做 scope、权限前提和排除条款预筛 | 来源可追溯；无未解决重复；明确不合资格则以证据结案 |
| **1 复核** | 枚举相关部署变体；核对源码、代理实现与 runtime bytecode 对应关系；逐条尝试推翻；区分受害者损害和攻击者获利 | 受影响部署集合明确；根因在实际部署上成立；未覆盖变体不得被写为已受影响 |
| **2 实证** | 钉块 fork，调用真实地址，覆盖攻击至退出路径；验证 pause/denylist 等实际相关控制；保存测试、日志和金额核算 | 预期断言执行且通过；链上状态可反算；无未说明的目标状态篡改；获利与损害分别量化 |
| **3 资格** | 按实证结果正式定级，逐条检查排除项；检索历史审计和 known issues；冻结规则依据 | scope 和影响匹配；排除项通过；查新记录完整；不夸称已证明绝对新颖 |
| **4 固化** | 一个根因一份英文报告；组装自包含 PoC 包；干净目录运行；生成清单、哈希和 zip；按已有授权提交至私有仓 | 干净目录运行成功；报告数字来自证据；依赖固定；包内无密钥；产物完整 |
| **5 自审** | 独立会话或 agent 从冻结包重跑，核查金额、前提和措辞，准备 triager 质疑及回答 | 独立复核通过；复核记录绑定最终包哈希；未解决异议为零 |
| **6 提交与防守** | 再查规则和渠道私密性，核对账号限额，准备 KYC，人工提交；录入回执，跟踪响应和申诉 | 提交编号、时间、渠道、最终包哈希和回执已入 ledger，才可标记 `SUBMITTED` |

补充实证约束：

- 钉块记录包含 chain ID、区块号、区块哈希、目标代码哈希、代理实现和工具版本。
- `deal`、`prank`、时间推进、oracle mock 等测试能力逐项声明；攻击者现实中无法取得的权限不能用于证明可利用性。
- `PnL` 分开列出收入、本金、费用和 gas；未知成本保留未知，不能默认零。
- pause/denylist 不存在时允许注明不适用，不要求制造无关测试。
- 误报测试必须证明具体阻断机制，例如权限检查、不可达状态或无法退出；“攻击脚本 revert”本身不是充分反证。
- 发现新代码、新规则或关键前提变化时，保留旧证据，使受影响 gate 及其后续 gate 失效。

## 4. CLI、模板与提交后管理

### 4.1 最小 CLI

使用一个 Python CLI，采用标准库处理路径、哈希、锁和原子写入；YAML 使用 PyYAML 安全加载，不自行实现 YAML 解析器。

```text
init       初始化计划工作目录
register   从原始 finding 登记
check      只读检查指定阶段的门槛
advance    验证后推进一个阶段
close      记录合并、不合资格、误报或平台结果
reopen     记录重开原因并使相关 gate 失效
resume     校验当前状态，输出阻塞项和下一步
index      重建派生索引
record     录入提交、响应或申诉回执
```

所有命令显式接受 `--case-root`，涉及 finding 的命令接受 `--id`。修改命令使用 `--expected-revision` 防止覆盖其他会话的更新。

检查输出提供人类可读摘要及 JSON：

- gate ID 和状态；
- 缺失产物；
- 哈希不匹配项；
- 未完成判断；
- 可执行的下一步。

退出码固定为：`0` 成功、`1` gate 未通过、`2` 输入或运行错误。没有 `--force` 越过证据门槛的接口。

哈希有效只证明产物未变化。gate 还必须核验其适用部署、输入依据和实质复核记录，不能把文件存在当作结论成立。

### 4.2 模板内容

- **Finding 模板**：主张、来源、部署、前提、反证尝试、影响、证据和下一步。
- **评估模板**：四支柱默认定义为“部署与可达性、根因与失效机制、实际影响与经济结果、计划资格与新颖性”；同时记录不利证据。
- **前置条件表**：逐项记录控制者、现实可达性、成本、持续时间、fork 如何满足以及排除条款引用。
- **英文报告模板**：summary、root cause、preconditions、attack path、impact、PoC、limitations 和可选 remediation 附件。平台字段映射由计划配置指定。
- **PoC 包模板**：测试源码、必要依赖、版本、运行说明、预期输出、清单与 `setup_and_run.sh`。
- **自审模板**：独立复现结果、金额核查、措辞修正、不利事实和未解决异议。
- **申诉模板**：平台理由原文、逐点回应、新证据、承认的限制和请求重新判断的事项。

PoC 包默认内嵌允许分发的源码依赖；外部前提限定为声明的工具链和 RPC。无法合法内嵌的依赖必须明确列出并阻塞“完全自包含”声明。运行脚本不得下载浮动分支或使用 `curl | sh`。

### 4.3 独立复核与冻结

复核者可以使用同模型，但必须来自独立会话或独立 agent。提供冻结包和必要规则，不提供作者预填的自审答案。

作者修改报告或 PoC 后生成新版本，不覆盖旧包。新版本必须重新完成包验证和独立复核；旧审核记录不自动继承。

提交后补充材料另存版本，原始提交包始终保留。

### 4.4 人工发送与后续跟踪

本版不实现平台自动提交、自动申诉或后台轮询。

- 每次恢复时计算待跟进事项和期限。
- SLA 只能来自规则或平台回执，未知时显示未知。
- 账号限额依据人工维护的完整提交记录；其他项目提交情况未知时，标记待确认，不能声称额度充足。
- KYC 仅记录状态，不保存证件。
- 推送前实时检查仓库为 `PRIVATE`；检查失败或可见性未知则阻塞推送。
- 交付前检查密钥、内部链接和跨报告引用；每份报告须能独立阅读。
- “已生成材料”不能记为“已提交”；发送结果不明时先核对平台，避免重复提交。

## 5. 验收与实施顺序

### 验收场景

使用标准库 `unittest`，在临时目录验证以下真实行为：

1. 正常 finding 逐阶段推进，缺少任一必要证据不能跳级。
2. 误报缺少 fork 反证不能关闭；RPC 故障保持阻塞。
3. 不合资格与技术误报分开记录，重复合并保留来源。
4. 修改 PoC、报告、部署或规则后，相关 gate 失效。
5. 两个会话同时写入时，一个成功，另一个获得明确冲突。
6. ledger 写入后索引更新失败，恢复能重建且不重复迁移。
7. 非法字段、路径越界、证据哈希错误和合并环被拒绝。
8. 独立自审绑定错误包、提交无回执、平台结果无依据时不能推进。
9. `REJECTED` 后可记录申诉并更新平台决定，历史不丢失。
10. 包在干净目录缺依赖时失败；正常包实际执行断言并留下可核验结果。

另用受控测试合约在本地链上构造成功与反证样例，验证完整流程；这验证工作流，不替代实际目标的主网 fork 取证。正式接入首个项目时，再完成一条真实候选和一条误报的试运行。

### 实施顺序

1. 固定 ledger、program 配置、状态迁移及 gate 契约。
2. 实现 CLI 的校验、原子迁移、恢复与索引，并通过状态测试。
3. 编写七阶段流程和模板，验证干净目录 PoC 包运行。
4. 编写精简 `SKILL.md`，按阶段加载详细规范。
5. 用独立会话做行为验收，最后接入真实项目。

首版不增加数据库、Web UI、审计器插件体系、自动提交服务或模型路由。保留文件作为权威状态，以可重现证据和明确门槛完成闭环。
